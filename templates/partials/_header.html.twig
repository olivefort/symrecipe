<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('home.index')}}">Symrecipe</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        {% if app.user %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('ingredient.index')}}">Ingrédients</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('recipe.index')}}">Recettes</a>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.pseudo}} </a>
            <div class="dropdown-menu">
              <a class="dropdown-item" href="{{ path('user.edit', {id : app.user.id})}}">Profil</a>
              <a class="dropdown-item" href="{{ path('user.edit.password', {id : app.user.id})}}">Mot de passe</a>
              <div class="dropdown-divider"></div>
              <a href="{{ path('security.logout')}}" class="dropdown-item">Déconnexion</a>
            </div>
          </li>
        {% endif %}
      </ul>
    </div>
    <div>
      <ul class="navbar-nav me-auto">
        {% if not app.user %}
          <li class="nav-item">
            <a href="{{ path('security.login')}}" class="nav-link active">Connexion</a>
          </li>
          <li class="nav-item">
            <a href="{{ path('security.registration')}}" class="nav-link active">Inscription</a>
          </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>